<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
	<Import Project="Directory.NuGet.props" />
	<PropertyGroup>
		<VersionPrefix>$(AppDomainAlternativeVersion)</VersionPrefix>
		<VersionSuffix Condition="'$(Configuration)' != 'Release'">1-at20230722033345</VersionSuffix>
		<VersionSuffix Condition="'$(Configuration)' == 'Release'">1</VersionSuffix>
	</PropertyGroup>
</Project>